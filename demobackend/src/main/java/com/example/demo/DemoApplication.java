package com.example.demo;

/**
 * Legacy placeholder kept so the existing source layout remains compilable.
 * The actual application entry point now lives in com.foodapp.FoodAppApplication.
 */
public final class DemoApplication {

    private DemoApplication() {
    }
}
